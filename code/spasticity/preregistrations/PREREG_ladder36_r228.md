# PREREG_ladder36_r228.md — the three unlooked-at severities

**Round 228.** ⛔ **Registered and hashed BEFORE extraction. Read-only, zero cells.**
*Where this document and any script disagree, THIS DOCUMENT DECIDES.*

---

## 1. Why

**A cold reader, asked what would move it toward believing the result is about spasticity rather than
about this corpus, named first: channel data at ×0.800, ×0.900 and ×0.950.**

⭐ **Those cells exist and are admitted 6/6 through Gate G** — `SPEED_LADDER_r217.json` →
`arms.*.n_gateG_admitted`. **Channels were computed for three of six severities and never for these
three.** *Read-only, no cells, and it doubles the weakness comparison set from 18 to 36.*

⛔ **This test can damage the headline and is registered on that basis.** *`hip_flexion_LmR` currently
separates 6 spastic against 18 weak with nearest seeds 1.0128° apart. Eighteen cells that have never
been looked at are added, and the fragile rung is already among the three we have: W870 clears the
control band by 0.0092°.*

## 2. What is computed

**All sixteen registered channels, window [1.00, 9.73], identical measurement path to
`channels_g2_r219.py`, for `R151W` (×0.800), `R169W090` (×0.900), `R169W095` (×0.950).**

**Then, on the extended set:** *(a) seed-level disjointness of `hip_flexion_LmR`, spastic against each
of the six severities and against all 36 pooled; (b) the exhaustive permutation null at the three new
severities, 924 assignments each; (c) the leave-one-seed-out classifier at 6 versus 36, restricted to
the four `(L−R)` channels as `PREREG_lmr_only_r223.md` registered.*

⚠ **The classifier's null becomes C(42,6) = 5,245,786.** *If exhaustive enumeration is infeasible the
result says so plainly and reports a sampled count with the number of samples stated. **No silent
substitution of sampling for enumeration.***

## 3. The three readings, fixed before the numbers exist

| | condition | registered reading |
|---|---|---|
| **A** | **disjointness holds across all 36 weak cells** | ⭐ **The finding survives a doubling of the comparison set at no cost to us, and the "six restarts against eighteen" objection becomes six against thirty-six.** *Said plainly.* |
| **B** | **some new severity overlaps the spastic range** | ⛔ **SHIPS AT FULL STRENGTH AND CHANGES THE HEADLINE.** *The claim becomes severity-conditional, and the honest form reports **which severities separate and which do not, with the seeds**.* ⛔ **The overlapping severity is NOT excluded.** *`cmd_1.30`'s exclusion is already in this paper; a second exclusion after an inconvenient result is the pattern a referee will name, and it is refused here in advance.* |
| **C** | **the new severities separate but shrink the margin** | **Report the new minimum gap as the headline**, per the "headline the smaller" rule already applied twice in this paper. |

⛔ **No fourth reading may be added after the numbers.**

## 4. What this cannot establish

- ⛔ **It does not add a second body, a second controller, or a second spastic magnitude.** *The severity
  control stays one-sided: the spastic arm has one viable KV and always will in this corpus.*
- ⛔ **It does not price the post-hoc restriction to the `(L−R)` family**, which remains unpriced.
- ⚠ **Six severities of one lesion type is still one lesion type.** *Doubling the weak arm does not
  broaden the spastic arm at all.*
- ⛔ **Nothing about patients.**
