# MACHINE-CHECKABLE COMPANION. Verify from THIS directory:  sha256sum -c CORPUS_MANIFEST.checksum.sha256
#
# The registration of record is CORPUS_MANIFEST.sha256, which is NOT edited, renamed or
# regenerated by this file. Its content is correct and its LINE ENDINGS are CRLF, so the
# filename it names parses with a trailing carriage return and sha256sum -c answers
# 'FAILED open or read' -- a sound registration that no standard checker can use.
#
# The digest below was re-derived from the live file and CONFIRMED IDENTICAL to the digest
# recorded in the original, character for character. This companion adds a readable form; it
# does not add, revise or supersede any claim.
#
# The original is evidence. Evidence is not edited to make a tool happy.
463b81a62c9ea921345aad97f806d62771acc765fbeab6a3831bf935cca1fcb0 *CORPUS_MANIFEST.json
